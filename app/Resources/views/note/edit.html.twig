{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Page</h1>

    {{ form_start(edit_form) }}

    {% form_theme edit_form 'form/bootstrap3_layout.html.twig' %}

        {{ form_widget(edit_form) }}
        <br>
        <input type="submit" class="btn btn-default" value="Submit Edit" />
        {{ form_end(edit_form) }}

        <br>

            {{ form_start(delete_form) }}
                <input type="submit" class="btn btn-default" value="Delete Page">
            {{ form_end(delete_form) }}

       <br>
        <a href="{{ path('note_index') }}" class="btn btn-default">Back to Notebook</a>

{% endblock %}
