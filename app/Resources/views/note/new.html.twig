{% extends 'base.html.twig' %}

{% block body %}
    <h1>Write a new note!</h1>

    {% form_theme form 'form/bootstrap3_layout.html.twig' %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-default" value="Write Notebook Entry" />
    {{ form_end(form) }}

    <br>
    <a href="{{ path('note_index') }}" class="btn btn-default">Back to Notebook</a>

{% endblock %}
